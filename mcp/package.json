{
    "name": "@pastebucket/mcp",
    "version": "0.1.0",
    "description": "Local MCP server and CLI that publish end-to-end encrypted documents to PasteBucket.",
    "private": true,
    "type": "module",
    "license": "MIT",
    "engines": {
        "node": ">=22"
    },
    "bin": {
        "pastebucket-mcp": "dist/mcp/src/server.js",
        "pastebucket": "dist/mcp/src/cli.js"
    },
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "typecheck": "tsc -p tsconfig.json --noEmit"
    },
    "dependencies": {
        "@modelcontextprotocol/sdk": "^1.30.0",
        "zod": "^4.0.0"
    },
    "devDependencies": {
        "@types/node": "^26.1.2",
        "typescript": "^7.0.2"
    }
}
